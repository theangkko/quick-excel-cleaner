# Drawing cleanup

Supported small-object cleanup targets:

- `oneCellAnchor`
- `twoCellAnchor`

The configured threshold is measured conservatively in pixels. Delete only anchors that are definitively within the configured threshold. Normal-size drawings must remain intact.
